## Debug Exercise
ugged.cpp — original code with an intentional defect
debugged.cpp — corrected version

In this exercise, I identified a logical error inside a nested loop that was causing an out-of-bounds array access. I isolated the defect using manual tracing and corrected the loop boundary condition to fix the memory corruption.
